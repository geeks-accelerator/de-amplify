# CLAUDE.md

Operational guide for working in this repo. For what the project *is* (the thesis, the document structure, the routes), read [README.md](README.md). This file is the conventions and gotchas that keep it from breaking.

## Standalone repo (hard rule)

This repository is self-contained and public. Do **not** add references to any other, sibling, or private repository: not by relative path (e.g. `../other-repo/...`), not by project name, and not in commit messages. If you need a concept that lives in another project, describe it inline here instead of linking to or naming the source. The git history was scrubbed of such references before this repo went public; keep it that way.

## House style (load-bearing)

- **No em-dashes.** The house style forbids the em-dash and the en-dash used as punctuation. Use commas, colons, parentheses, or restructure the sentence. This applies to content, code comments, and commit messages alike. Curly/smart quotes are also out; use straight quotes. In a TS/TSX string that needs a straight double quote, escape it (`"\"Show me less\""`) rather than reaching for a curly pair.
- **Checking it (the naive grep lies on macOS).** `grep -l $'[‘’“”]'` over-reports badly here: BSD bracket collation matches U+2014 and other punctuation, so it flags files with zero curly quotes and sends you chasing ghosts. Use one exact codepoint per pattern under the C locale:

  ```bash
  LC_ALL=C grep -rn --exclude-dir=sources --exclude-dir=reviews \
    -e $'—' -e $'‘' -e $'’' -e $'“' -e $'”' src/ content/ docs/ *.md
  ```

  Sweep `docs/` and the root `.md` files too, not just `src/` and `content/`: the last em-dash found in this repo was in `docs/plans/`, which a `src content` only scan never reaches. The two `--exclude-dir`s are the exemptions below. This file will always self-match (it contains the literal characters in the line above); that is the one expected hit.

  **Directories named `sources` or `reviews` are deliberately exempt**, because their punctuation is evidence rather than prose: `docs/distillations/sources/` (verbatim official transcripts, whose GPO punctuation the ledgers reproduce exactly), `docs/distillations/reviews/` (raw output from external review models, referenced as `raw_audit_trail` in ledger frontmatter), and `docs/reviews/` (whole-repo external audits, filed verbatim). Rewriting punctuation in any of them is editing a record of what someone else said. Everything else, including `src/`, `content/`, `docs/proposals/`, `docs/plans/`, and this file, is held to the rule.

  **Note the mechanism, because it is easy to break by tidying.** `--exclude-dir` matches a directory's *basename at any depth*, which is why `docs/reviews/` inherited an exemption written for `docs/distillations/reviews/` without anyone adding it. That is convenient and slightly accidental: if the sweep is ever tightened to match full paths, every exempt directory has to be listed explicitly or CI goes red on the first quoted em-dash. Verified 2026-08-14 with a positive control (the filed audit really does contain 4 em-dashes and 3 curly apostrophes; the sweep passes anyway).
- **Commit messages**: lowercase, colon-prefixed by area (`site:`, `content:`, `docs:`, `scorecard:`, `proposal:`), imperative mood, body wrapped. End with the `Co-Authored-By:` trailer when co-authored. Commit and push only when asked.
- **`main` is protected: it takes pull requests, not direct pushes.** A direct `git push origin main` is rejected with `GH006: Protected branch update failed`, and the rule applies to admins too. Work on a branch, open a PR, merge it (0 approvals required, no required status checks, so a solo maintainer is not blocked, just routed). The `(#25)` / `(#27)` suffixes on subject lines in `git log` are squash-merge artifacts, not something to type by hand. Merging is what deploys: Railway builds `main`.
- **Voice**: the copy is confessional, direct, terminal/mono in feel (Geist Mono, the void / brake-red palette). Match the surrounding text; do not turn it into marketing.
- **Contrast floor: real text clears WCAG AA 4.5:1 against the void.** The muted palette is the look, but the bottom of the opacity ladder was illegible (`text-bone/30` measured 2.28:1), so on 2026-07-24 it was remapped repo-wide: `bone/20 -> 52`, `30 -> 55`, `35 -> 57`, `40 -> 60`, `45 -> 63`, plus `brake/70 -> 90` and `signal/60 -> 80` on real text. **`text-bone/50` (4.51:1) is the floor; do not go below it for anything a reader reads.** All the site's type is under 24px, so the large-text 3:1 allowance never applies. Genuinely decorative glyphs (the footer `/` separators, the terminal `>` prompt) keep their low opacity and carry `aria-hidden="true"` instead; the one other exemption is the `prose-li:marker:` bullet, which is `::marker` decoration on a semantically-marked list. Adding `text-bone/40` for new copy silently reopens the whole class. **Keyboard focus is part of this floor and lives in one place, `src/lib/focus.ts` (`FOCUS_RING`).** It was briefly copy-pasted into four components, which is how a repo-wide rule stops being repo-wide: someone adjusts one copy and the other three keep the old value while still looking deliberate. Use `:focus-visible`, never `:focus`, so a mouse click does not paint a ring nobody asked for; brake at full opacity measures 6.21:1 against the void.

## Content pipelines (know which one you are in)

- **Policy paper and appendix.** `content/proposal.md` and `content/notes.md` are the frontmatter-stripped *bodies* of the review copies in `docs/proposals/`. Edit the review copy, then regenerate the body. Do not add nav links or frontmatter to `content/*.md` (react-markdown renders them raw).
- **`docs/proposals/` is not one thing, and the difference is dangerous.** It holds four kinds of file that look identical: two **live review copies** (the two above), three **live deck sources** for `/for` (read directly by `llms-full.txt`), one **open proposal** (scorecard v2), and at least one **superseded artifact**: `2026-07-15-the-social-media-addiction-lawsuits-explained.md` was the original review copy of `/lawsuits` from before that page moved to the ledger-first pipeline. It has not been maintained since, and as of 2026-07-25 it differs from the live page in about fourteen passages where the live page is the correct one. **Regenerating `content/lawsuits.md` from it would silently revert months of ledger-first corrections.** Its frontmatter now says so; check for a `superseded:` key before treating anything in that folder as a source.
- **Lawsuits and hearings are ledger-first.** `docs/distillations/*.md` are the evidence-tiered claim ledgers and are the source of truth. `content/lawsuits/*.md` (the case files), `content/lawsuits.md` (the hub), and `content/hearings.md` (the hearings hub) are *seeded from* the ledgers, curated, not a 1:1 render. Corrections land in the ledger first (verified against a primary source), then the page is re-seeded on a dated pass. Never fix a fact only on the page.
- **The ledgers are also published directly.** `/distillations/<slug>` (plus a raw `/distillations/<slug>.md` per ledger, and inclusion in `/llms-full.txt`) renders each ledger via the section whitelist in `src/lib/distillations.ts`: reader summary, brake-integrity relevance, the tiered claims, the full quote-bank, tensions, plus Sources rebuilt from frontmatter and the lawsuit ledgers' own Tier key paragraph. The TLDR block, Discrepancies, and Coverage note stay out (authoring/QA scaffolding). So a ledger correction publishes to `/distillations` automatically with no re-seed; only the curated pages need the dated pass. A new ledger needs **both** an entry in the `DISTILLATIONS` list in that lib file **and** a filled-in TLDR block (see the two bullets below); registering it without the TLDR variants yields a blank index card and an OG card that falls through to the generic "The evidence ledger" title.
- **Verbatim TLDR surfaces.** Each ledger has a TLDR block of budgeted variants (label / nav-label / og-title / search-snippet / ledger-snippet / one-sentence / one-paragraph). Those exact strings are wired verbatim into consumers, **one consumer each**: label -> hub card (`content/lawsuits.md`, the `content/hearings.md` table), nav-label -> the `/distillations` index card and breadcrumb, og-title -> the share card, search-snippet -> the **curated page's** meta description, ledger-snippet -> the **`/distillations/<slug>` page's** meta description, one-sentence -> JSON-LD description, one-paragraph -> page opener. The one-consumer rule is load-bearing for SEO, not just for drift: search-snippet briefly had two consumers (the case page copied it verbatim while the ledger page parsed it), which shipped **identical meta descriptions on two indexable URLs about the same case**. Ledger-snippet exists to break that tie and is optional, because the hearing ledgers share one `/hearings` page and so have no collision to break. Everything except the hub card is parsed at build by `src/lib/distillations.ts`. Copy verbatim; do not hand-adapt a summary (an adapted copy is a third unvalidated variant and reopens drift). If a consumer needs a length the ledger lacks, add that budgeted variant to the ledger, not to the page.
- **`DISTILLATIONS` in `src/lib/distillations.ts` holds routing config only** (slug, kind, tierDown, related). Do not put a reader-facing string there. Nav-label and og-title used to be hand-authored in that array, which made them unvalidated variants of summaries the ledgers already owned; they are now parsed from the ledger like every other variant. A new ledger needs its TLDR block filled in, not new strings in the array.
- **The two action pages share their data with their raw markdown, and must keep doing so.** `/scorecard` and `/report` are hand-built React with no `content/` body, but they also have raw `.md` twins (`/scorecard.md`, `/report.md`) and appear in `llms-full.txt`. **The criteria and the form vocabulary live once, in `src/lib/actionPages.ts`**, and the page, the raw route and the bundle all render from those arrays. Do not hand-write a markdown copy of either page: the scorecard's seven criteria are already held in lockstep with section 3 of the policy paper, and a third hand-maintained copy is precisely the drift this repo builds checkers to prevent. Changing a criterion in that lib changes all three surfaces together; it does **not** change the paper, which stays a deliberate human edit. Note that `ReportForm.tsx` is a client component and imports from this lib, so keep the module free of anything heavy: verified 2026-08-14 that tree-shaking drops the scorecard half and the markdown generators from the client bundle (probe on string literals, not on symbol names, which minifiers rename).
- **Hand-built React** (not markdown): the homepage, `/report`, `/scorecard`, `/for`, `/remixes`, and `/distillations` (which renders the ledgers).
- **The table of contents is server-rendered, and its anchors must agree with `rehype-slug` or they silently do nothing.** `src/components/Toc.tsx` renders a collapsed contents list on `/proposal`, `/notes`, `/lawsuits` and `/hearings` from the same markdown string the renderer already receives, so it ships no client JS (a TOC that needed a hook would reship the parser the rule below exists to keep out). `src/lib/toc.ts` reimplements github-slugger's algorithm, **including its `-1`/`-2` duplicate suffixes**, because that is what `rehype-slug` uses to build the heading ids. If the two ever disagree, every link becomes a no-op: the page still renders, the anchor still looks fine, and nothing scrolls, which no build can catch. Verified against the ids actually in the DOM (13 of 13 on `/lawsuits`, 15 of 15 on `/proposal`) and unit-tested for the duplicate path, which no current document exercises. It also skips fenced code blocks, so a `## ` inside a shell example is not mistaken for a heading.
- **The markdown renderer is a server component, deliberately.** `src/app/proposal/ProposalContent.tsx` renders every markdown document (`/proposal`, `/notes`, `/lawsuits`, the case files, `/hearings`, the ledger pages). It has no `"use client"` and must not get one: react-markdown 10's default export has no hooks, so it renders server-side fine, and adding the directive reships react-markdown + remark-gfm + micromark + rehype-slug (measured: **42 KB gzipped**) to every document route *and* serializes the whole markdown string into the flight payload on top of the HTML it already rendered. There are **three** client components: `src/app/report/ReportForm.tsx`, `src/components/SunoEmbed.tsx`, and, since 2026-08-14, `src/components/HeaderNav.tsx`. **The rule is about shipping a parser, not about banning interactivity**, so read it that way before adding a fourth. `HeaderNav` exists to put `aria-current="page"` on the active link, which needs `usePathname`; it imports no library, and `next/link` already puts the App Router context in the client bundle on every page, so the measured cost was **301 bytes gzipped** (177,796 -> 178,097 total client chunks, built both ways from a cold `.next`). That is 0.6% of the 45.8 KB the rule exists to keep out. A candidate fourth that pulls in a dependency is a different question from one that does not; measure it the same way rather than arguing from the rule's wording.

## Evidence discipline (this is a public legal site)

- A legal or factual claim must trace to a **primary source** (a court order or filing, the docket, an official government release) to be stated as fact. Secondary coverage gets tiered down and flagged.
- **Posture precision.** "Announced intent" is not "filed" is not "affirmed"; a summary-judgment ruling is not a motion to dismiss; a jury verdict is not "upheld on appeal." The project was burned once by a "$6M upheld on appeal" error; do not repeat that class.
- **Event date is not coverage date.** An order "filed June 29" may be "announced June 30"; record the event date, note the coverage date. This error recurred repeatedly, so watch for it.
- Everything carries an **as-of date**. Figures date quickly.
- **A search that returns nothing is a claim about your query, not a fact about the record.** Prove the query with a positive control before reporting an absence: run it against a string you already know is in the source, and if the control comes back empty the query is broken, not the corpus. On 2026-07-25 a case-sensitive grep for prepared-statement markers returned zero against a record containing seven, and the zero was believed because it agreed with the expected answer. A hit is self-evidencing; a miss evidences only that you trusted the search. Bias ad-hoc checks toward over-matching: a false alarm gets investigated, a false all-clear gets published.
- **An official channel is not automatically the fullest record, and "best source" is not the same as "most complete".** Established 2026-08-20 on the attorneys general press conference of 2026-08-18. The California Department of Justice, whose office held it, published 23:26 on its own channel: the recording ends on the words inviting questions and **omits the entire Q&A**. PBS NewsHour published 52:20 of the same event including all of it, and the exchange that answered a live Tension in the MDL ledger falls in the 29 minutes the official version does not contain. Three separate passes retried the official URL over two days without once asking whether another recording of the same event existed, because official outranks secondary in this project's tiering and that ranking was silently read as completeness. **They are different properties.** For any public event on video, enumerate the recordings and compare durations before choosing which to keep; it is one search and it costs nothing. Related: **a caption track listed by `yt-dlp` under "subtitles" rather than "automatic captions" tells you the track was uploaded, not that a human wrote it.** The uploaded track for that event was machine output, and less accurate than hosted ASR on the same audio. There is no cheap flag for provenance here; read a passage you can check.
- **The quote-fidelity check is sensitive to five normalizations, and applying some but not all moves the error rather than removing it.** A full re-run over the four hearing ledgers verifies **245 of 246** spans, but only with all of them: fold curly punctuation (the testimony PDFs use curly apostrophes, the ledgers straight), split on ellipses (an elided quote is fragments, not one string), rejoin GPO's hyphenated line wraps (`core well- being` in the cache is `well-being` in the ledger), fold typographic ligatures (scanned court filings carry `ﬁ` as one character, so `sufﬁcient` never matches `sufficient`), and strip the **line numbers US court filings carry in the left margin of every line**, which `pdftotext` keeps and whitespace-collapsing then injects mid-sentence (`devices other than 11 computers and smartphones`). Miss one and you get failures that read exactly like fidelity defects, and the ligature case is the worst because the two strings look identical on screen. Details and the single known real deviation are in `docs/distillations/sources/README.md`. **This is now executable: `npm run check:quotes`.** Do not re-derive it by hand or with an ad-hoc grep; the script applies all five normalizations together, which is the whole point.
- **Verbatim is not the same as attributed, and only the first is machine-checked.** The quote-fidelity checker confirms a quoted span is an exact substring of the cached transcript. It cannot tell you *who said it*. A 2026-07-24 re-seed put a witness's line in a senator's mouth on `/hearings` and passed every check, because the string really was in the transcript. In the hearing ledgers' witness quote-banks the senator named in each parenthetical is the **questioner**, never the speaker; the speaker is the subsection. When re-seeding a quote, confirm the speaker against the transcript by eye, not by substring.
- **An English rendering of a non-English source is a translation, not a quotation, and must never appear inside quotation marks attributed to the court.** The two Dutch judgments in `bits-of-freedom-v-meta` are the corpus's first non-English sources and they set the convention for every future one: **the quoted span in the ledger is the original-language text**, so `npm run check:quotes` can verify it against the cache, with the English beside it explicitly labelled as a translation. On reader-facing pages, paraphrase in the site's own voice and cite the paragraph number rather than quoting Dutch at a reader who cannot read it. This is not pedantry about attribution, it is what makes the check mean anything: had that ledger quoted the English, every span in it would have had to be allowlisted and the checker would have proved nothing about its fidelity to the court.
- **Punctuation that is not in the source goes outside the closing quote.** `"...the absence of Meta's competitors in this litigation".` not `"...in this litigation."` The verified ledgers already do this; the FTC ledger's Claims section did not, and that is exactly how four spans drifted while looking perfectly ordinary. A period inside the quote is a claim that the source's sentence ended there.

## Adding or changing a route

Wire it into every discovery surface or it drifts out of sync:

- `src/app/sitemap.ts`, `src/app/llms.txt/route.ts`, `src/app/llms-full.txt/route.ts`, and the `_links` (plus a `skill` where it fits) in `src/app/agent-card.json/route.ts`. `_actions` in the agent card is capped at five, so extend `_links`, not `_actions`. A rendered document also gets a raw-markdown route (e.g. `/hearings.md`, `/distillations/<slug>.md`) with RFC 8288 `Link` headers (`up` / `index` / `canonical`), and is listed in llms.txt. **Since 2026-08-14 that includes the two ACTION pages**: `/scorecard.md` and `/report.md`, which are not read off disk but GENERATED from `src/lib/actionPages.ts`, the same arrays the pages render. They exist because the agent card's `_actions` send agents to run the scorecard and file a report, and the bundle those agents are told to read used to contain neither. Raw `.md` routes are deliberately **not** in the sitemap: they carry `rel="canonical"` back to the styled page, so indexing authority consolidates there rather than splitting across two URLs for one document. Note that `llms-full.txt` reads from **two** trees: the `content/` bodies plus, for the three `/for` briefing decks, their source files in `docs/proposals/` (the decks are gamma.app source with no `content/` body, but they are rendered documents, so "every rendered document" has to include them). The agent card's per-ledger `_links.ledgers` is generated from the `DISTILLATIONS` registry rather than hand-listed, so a new ledger wires itself in; the `/for` and `cases` branches are still hand-listed. **`llms.txt` follows the llmstxt.org format, verified against the spec on 2026-08-14**: every resource is its own markdown list item (a hyperlink, optionally `:` and notes), never several comma-separated links in one bullet, and the trailing `## Optional` section is the spec's own convention for resources an agent can skip when its context budget is tight. The rendered-HTML twins, the song page and the repository live there; the substance does not. The `## Pages` and `## Source` sections no longer exist. **As of 2026-08-13 the `llms.txt` ledger list is generated too**, grouped by `kind`, labelled with each ledger's own `navLabel` and counted from the registry: it used to be nine hand-written labels plus the literal word "Nine", which is three unvalidated variants per ledger and a count that goes stale silently. The agent card's `as_of` is likewise **derived** (the newest ledger `as_of`) rather than the literal it was, which had already gone stale by a week.
- If it is a content page: add `Article` + `BreadcrumbList` JSON-LD and **per-page `openGraph` / `twitter`** metadata. The OG image is a **colocated `opengraph-image.tsx`** in the route folder (it calls the shared `ogCard` in `src/lib/og/template.tsx`, brake or signal accent); Next wires it into `og:image` automatically, so the per-page `openGraph` object does **not** set `images`. A dynamic route generates one OG per param via `generateStaticParams` (see `/distillations/[slug]`). Do not build class names dynamically (`text-${accent}`); Tailwind cannot see them, so use literal ternaries.
- Dates in JSON-LD and the sitemap come from **`content-dates.json`**, a committed manifest of git author dates, via `src/lib/contentDate.ts`. Not `fs.statSync().mtime` (a fresh checkout resets mtimes to deploy time, restamping every page on every deploy) and **no longer a live `git log` at build time** either, because that silently failed on Railway for weeks. **If you change content, run `npm run dates` and commit the manifest with it**; `npm run check:dates` fails the pre-PR check if you forget. Do not reintroduce mtime, and do not reintroduce a build-time shell-out to git as the primary source.

## The scorecard is the standard (lockstep)

`/scorecard` renders the seven brake-integrity criteria from the policy paper section 3. **Changing the scorecard means changing the standard**, in the paper and on the page together. There is an open revision proposal, `docs/proposals/2026-07-16-scorecard-v2-brake-performance-test.md`; the current seven-part test stays operative until that clears review, so do not quietly edit the dimensions out of band.

## Dependency pins (load-bearing; do not casually bump)

The stack runs current majors (Next 16, React 19, Tailwind 4, ESLint 9, TypeScript 6), but a few pins are deliberate and will break the build if "helpfully" upgraded:

- **ESLint stays at 9, not 10, and the blocker is `eslint-config-next`, not `typescript-eslint`.** On paper ESLint 10 is supported: `eslint-config-next` 16.3.1 declares `eslint: ">=9.0.0"` and `typescript-eslint` 8.65.0 declares `eslint: "^8.57.0 || ^9.0.0 || ^10.0.0"`. Both peer ranges are aspirational. `npm run lint` on ESLint 10 dies immediately with **`TypeError: scopeManager.addGlobals is not a function`**, thrown from ESLint's own `source-code.js` at `addDeclaredGlobals`.

  **This entry previously blamed `@typescript-eslint/scope-manager` for not implementing that method. That was wrong, and it sent at least one investigation down the wrong path.** 8.65.0 does implement it (`addGlobals(names) { this.globalScope?.addVariables(names); }`, in `dist/ScopeManager.js`), and so does `eslint-scope` 9.1.2, which is exactly what eslint 10.8.1 depends on. Bumping typescript-eslint fixes nothing; 8.66.0 and 8.67.0 exist and are irrelevant to this.

  **The real cause, established 2026-08-14 by isolating it.** `eslint-config-next/core-web-vitals` applies **its own parser to every file**, `.mjs` included, in its first config block: `files: ["**/*.{js,jsx,mjs,ts,tsx,mts,cts}"], parser: eslint-config-next/parser`. That parser (Next's own Babel-backed one, `meta.name` = `eslint-config-next/parser`, version tracking `next`) returns a scope manager predating ESLint 10's `addGlobals` API. That is why the `overrides` block never helped: it pins the nine `@typescript-eslint/*` packages, and none of them is in this path. The earlier note that the failure reproduced "with and without the `overrides` block" was true and simply not evidence about typescript-eslint.

  **The one-line proof, if you need to re-establish it:** run ESLint 10 against a trivial `.mjs` file with a flat config that sets no parser. It exits 0. ESLint 10 is fine; the Next config is not.

  **So the retest condition is not "has typescript-eslint fixed its scope-manager".** It is **"does `eslint-config-next` support ESLint 10 yet"**, and since that package is pinned exactly alongside `next`, the answer arrives with a Next release, not a lint release. Dependabot #36 and #45 were the same PR twice; **#45 was merged on 2026-08-14 with its CI already red and was reverted the same day.** Keep future ones closed until a Next release says otherwise, and re-test with `npm ci && npm run lint` rather than trusting either peer range.

  **Note also that production is unaffected either way**: `railway.toml` runs `npm install && npm run build` with no lint step. What a bad ESLint bump breaks is CI and local linting, and a permanently red `main` is how a check stops being believed.
- **`next` is pinned exactly, so its transitive advisories are yours to track. Bumped 16.2.12 -> 16.3.1 on 2026-08-13 to clear four high-severity ones.** `next` carries `sharp` as an optional dependency for image optimization, and 16.2.12 declared `sharp: ^0.34.5`, which is inside the vulnerable range for four libvips CVEs. Practical exposure here was close to nil (the site uses no `next/image`, sets no `images:` config, and the OG cards go through `next/og`, which is satori/resvg rather than sharp), but the advisories sat in the production dependency tree, and **because `railway.toml` deletes the lockfile before install, the exact version in `package.json` is the only thing that pins them**. 16.3.1 declares `sharp: ^0.35.3` and `npm audit` returns clean. Note the target: Dependabot's #44 proposed 16.3.0, and the house rule is to land the newest coherent release, so this went to 16.3.1 (verified to exist, along with `eslint-config-next@16.3.1`, before bumping). `npm ci` after any dependency merge, or you verify a tree the repo no longer declares.
- **`typescript-eslint` is pinned via `overrides`, now at 8.65.0 (was 8.59.4).** The old pin existed because 8.60+ published against sibling packages that were never published. **That is fixed upstream:** on 2026-08-02 all ten siblings resolve at 8.65.0 (`eslint-plugin`, `parser`, `utils`, `typescript-estree`, `type-utils`, `scope-manager`, `visitor-keys`, `types`, plus `project-service` and `tsconfig-utils`), and `npm ci` + `npm run lint` + `npm run build` all pass on ESLint 9. **One new ceiling this introduces:** 8.65.0's `typescript` peer is `>=4.8.4 <6.1.0`, so this pin now holds the repo under TypeScript 6.1. Note that for the next TS bump; it is a real constraint, not a warning.
- **`tailwindcss` + `@tailwindcss/postcss` are no longer pinned to 4.3.0.** They were, because 4.3.1 through 4.3.3 published against sibling packages that did not exist. **That has since been fixed upstream**: Dependabot #28 moved both to **4.3.3** on 2026-07-26, and on 2026-07-26 a clean `npm ci` resolves, `npm run lint` and `npm run build` pass, and a live contrast audit over 177 text nodes finds zero failures with the opacity colours still emitting. So the bump is fine and the old pin is history, recorded here so nobody re-pins on a warning that no longer holds.
- **A trap that bit during that check, worth generalizing.** After the bump, `package.json` and the lockfile said 4.3.3 while `node_modules/` still held 4.3.0, so local builds kept passing against a version the repo no longer declares. Railway installs clean, so production was building something local never had. **After any dependency merge, run `npm ci` before trusting a green build**; `npm run build` alone will happily verify the wrong tree.
- **TypeScript stays at 6** (no stable 7 exists yet). Lint runs via `eslint .` (flat config in `eslint.config.mjs`); `next lint` was removed in Next 16.

- **The lockfile does not constrain production, and this is easy to forget.** `railway.toml`'s build command is `rm -f package-lock.json && npm install && npm run build`. The lockfile is **deleted before install on every deploy** (deliberately, so native deps resolve for Linux rather than macOS). So production resolves fresh from the semver ranges in `package.json` each time, and a lockfile that pins a transitive dependency pins nothing in production. **The only real production pins are the exact versions written in `package.json`** (currently `next`, `eslint-config-next`, `tailwindcss`, `@tailwindcss/postcss`, and everything in `overrides`); every caret range can drift on any deploy without a commit. This does not conflict with the `npm ci` rule above, it explains it: `npm ci` locally proves the lockfile tree, and the lockfile tree is the closest local approximation of a resolution Railway will redo from scratch.

Dependabot keeps proposing majors and its exact version numbers are sometimes phantom (they 404). Before merging one, confirm the target and its transitive deps actually resolve; land on the newest coherent release, not Dependabot's literal number. The dev CSP grants `unsafe-eval` only when `NODE_ENV=development` (React 19 dev tooling); the production CSP stays tight.

## Build, deploy, verify

```bash
npm run dev     # http://localhost:3333
npm run build   # typecheck + compile; run before committing nontrivial changes
npm run lint    # eslint . (flat config); run before opening a PR
npm run start   # serve the production build

npm run check:ledgers   # verbatim-TLDR drift; run after ANY ledger or curated-page edit
npm run check:quotes    # quoted spans verbatim vs the source caches; run after ANY ledger edit
npm run check:surfaces  # share cards + structured data vs the record; run after ANY case-status change
npm run check:distillations  # internal consistency of the single-source research corpus
npm run check:issues    # docs/issues schema + index; run after adding or closing a standing thread
npm run check:guards    # every guard above is listed on all four surfaces that list guards
npm run dates           # regenerate content-dates.json; run after ANY content edit
npm run check:dates     # fails if that manifest drifted from git history
```

- **`npm run check:ledgers` needs Node 22.6 or newer, and the repo's `engines` field does not say so.** It passes `--experimental-strip-types` because it imports the real `src/lib/distillations.ts` instead of reimplementing the TLDR parser, and that flag did not exist before 22.6. On Node 20 it dies with `node: bad option: --experimental-strip-types`, which reads like a broken script rather than a version floor. `engines` stays at `>=20` because that is right about the **app** (production runs `nodejs_20` and never runs this script, and raising it would put a false constraint on Railway's install), so the floor lives here and in CI instead. This was found the honest way: the first CI run ever executed went red on it.
- **CI runs all of these, and it did not used to.** `.github/workflows/ci.yml` runs lint, build, **every `check:*` script** and the house-style sweep on every PR. It said "the three checks" until 2026-08-20, by which point there were six; the count is deliberately not restated here now, because `npm run check:guards` asserts that the list in the block above matches `package.json` and the workflow. It is **two jobs on two Node versions on purpose**: `build` on Node 20, because that is what production runs and a green build on any other version could be one Railway cannot reproduce; `guards` on Node 22, for the flag above. `fetch-depth: 0` is on the guards job only, because `check:dates` compares against real git author dates and a shallow clone would report drift on every path. A second, non-gating job runs `npm audit --audit-level=high`: dependency advisories are worth seeing on every PR but should not block an unrelated content fix, and `package.json`'s exact pins are the only real production pins because the lockfile is deleted before install. Until 2026-08-13 every guard in this repo was a command a human had to remember, which is a strange gap in a project whose whole discipline is catching drift that looks exactly like correctness.

- **`npm run check:ledgers` guards the one-consumer rule where a human does the copying.** Most TLDR variants are parsed from the ledger at build, so they cannot drift; four are hand-copied and those are exactly the ones that have drifted. It asserts, for all eleven ledgers: `One line (label)` appears verbatim in its hub (`content/lawsuits.md` or `content/hearings.md`), `One paragraph` in the curated case page's opener, `Search snippet` in the route file's `DESCRIPTION`, `One sentence` in that file's JSON-LD `description`, and, in the negative direction, that `Search snippet` does **not** also appear in the page body and `Ledger snippet` does **not** appear in the case route file (that pair is the two-indexable-URLs-same-description bug). It imports the real `loadDistillation` rather than reimplementing the TLDR parser, so the checker cannot disagree with the build. Exit 1 with the offending ledger, variant, consumer, and the ledger's own string on failure. **The fix direction is always ledger first, then re-seed the consumer**; the failure message says so, because editing the ledger to match a stale page is the tempting wrong move.
- **Two proven catches, and one it found on its first run.** Fault-injected and confirmed to fail on: the exact 2026-07-25 K.G.M. defect (page re-seeded everywhere except its opener), a one-hyphen hand-adaptation of a route `DESCRIPTION`, and the search snippet copied into a page body. On its first real run it caught a live orphan: the EU ledger's `One line (label)` had no consumer at all, because the hub carried a hand-written sentence instead of the label. **A variant with no consumer is a silent unvalidated string**, which is the same failure wearing different clothes, so the hub now carries it verbatim.
- **`check:ledgers` also asserts every registered ledger has a raw `.md` route, added 2026-08-13.** This closes the one surface a new ledger did **not** wire itself into. The agent card's `_links.ledgers` is generated from `DISTILLATIONS` and emits a `markdown:` URL for every ledger, but `/distillations/<slug>.md` is a hand-created route folder per slug, so registering a tenth ledger would have published a machine-readable link to a 404 while the page, the OG card, the sitemap and `llms-full.txt` all self-wired. Note that the obvious collapse does not work: Next only treats a path segment as dynamic when it both starts with `[` and ends with `]` (see `getSegmentParam`), so a `[slug].md` folder is a literal path, not a dynamic route. The nine near-identical route files stay; the checker is what makes forgetting the tenth loud.
- **`npm run check:quotes` is the quote-fidelity check, and until 2026-08-13 it was prose.** `scripts/check-quotes.mjs` verifies every double-quoted span in a registered ledger as a verbatim substring of its committed cache in `docs/distillations/sources/`, applying all five normalizations at once (curly punctuation, elision, GPO hyphenated line wraps, ligatures, court-filing line numbers). **All eleven ledgers are registered as of 2026-08-18; there are no unregistered ledgers left**, and the line the script used to print naming them is gone because there is nothing to name. Two known deviations, each recorded in the script with its reason. **The exact span count lives in one place, the coverage line of `docs/distillations/sources/README.md`, and the script asserts it on every run.** It does not live here, because it was written here once, as 476, and was stale within hours (483 by the same evening): a count in guidance prose is exactly the drift this repo's checkers exist to prevent, and the fix is the same as the corpus census, one declared home that a tool recomputes. A ledger can be registered whole, with an allowlist for deliberate drafter prose (the Nov 2023 and EU ledgers), or scoped with `sections` to just the part a cache covers (New Mexico, whose pre-judgment history quotes uncached coverage, plus Bits of Freedom and the FTC ledger). **Scoping is the check's own blind spot and it has already hidden real defects.** The FTC and Bits of Freedom ledgers were first scoped to `Quotes`, on the reasoning that the quote-banks hold the record quotes; the **Claims** sections quote the same caches, and being unchecked had absorbed sentence punctuation *inside* four quotation marks (the Eighth Circuit's `procedural error.` written as `procedural error,`, the Chitika order's `their choice(s), and shall remain` written as `their choice(s).`). Widening both to `["Claims (the ledger)", "Quotes"]` on 2026-08-14 raised coverage by roughly thirty checked quotations and surfaced all four at once. **Scope to what the cache covers, not to where you expect the quotes to be**, and re-read the exclusion whenever a ledger grows. **That rule then failed again on 2026-08-18, in the pass that was writing it down**: `mdl-3047` was registered with four caches and scoped to section (i), where the new trial-day quotes were, while section (e) had meanwhile acquired three claims quoting Dkt 473 from that same cache list, carrying the pass's most consequential correction. An audit found it; the check could not, because it cannot see what it is scoped away from. **The scope gets written when the cache is added and is never re-read when the ledger grows, and that is now a twice-proven failure rather than a caution.** Three things about it are deliberate: it **self-tests its matcher against known answers and aborts** rather than reporting a clean corpus it cannot actually read (a broken matcher reports green, which is this repo's recurring failure); a **declared-but-missing source or allowlist is a loud failure**, because the first draft of the script silently fell back to an empty allowlist and invented five phantom failures; and it **prints per-ledger span counts and the exact scoping on every run**, because a silent drop in one ledger is how a scoping bug hides while the total still looks healthy. Fault-injected and confirmed to fail on a one-word change to a real quote, and on a fabricated holding inside the New Mexico judgment section. On its first run against the EU ledger it caught a live defect: a Commission finding quoted as "Can be easily dismissed" when the release reads "can be easily dismissed" mid-sentence, which is the same class as a fabricated quote and invisible by eye. It proves **verbatim, never attribution**, which stays a by-eye check. **Since 2026-08-20 it also refuses ASR outright**: any declared source whose filename ends `-asr.txt` is a hard failure, with controls in both directions in the self-test. The corpus acquired its first machine transcription that day (the attorneys general press conference, whose Q&A exists in no other reachable form) and the danger is specific: verifying a quotation against a machine transcription proves that two machines agree, goes green while doing it, and every other checker here would agree with it. Such a file is a **pointer to a place in a recording**, never a text to quote; cite it with a timestamp and paraphrase, the same treatment the Dutch judgments get for the same underlying reason.
- **Closing the last three unregistered ledgers on 2026-08-18 caught six more defects and one bad cache, which is the argument for registration rather than a bonus.** Caching Meta's Dkt 455 (the source of the $1.4 trillion figure, cited for a month but never cached) showed the MDL ledger quoting the states' penalty exhibit by a title read off the **docket listing** rather than the title the filing gives it, and lowercasing that filing's sentence-initial capital. Caching the LA Superior Court's public notice showed the California ledger putting its own comma inside a quotation of the case caption and quoting `Hon. Carolyn B. Kuhl` where the notice reads "Hon. Judge Carolyn B. Kuhl". **A citation is not a cache**, and the gap between them is where all four lived. Registering Tennessee found nothing wrong in the ledger and instead found a fault in a **cache**, a first: the complaint's `-raw` extraction carries the tagged-PDF artifact `Lbl` 364 times, interleaved with the page footer at page breaks, and at paragraph 411 that lands mid-word so a faithful span cannot verify. That is a **sixth** failure mode alongside the five normalizations and no normalization reaches it; it is recorded in `KNOWN_DEVIATIONS`, not in an allowlist, because an allowlist entry would assert the span is deliberately not from the cached source and it is. Grep `^Lbl` on any new tagged-PDF cache the way you grep the TIFF marker on a new GPO transcript.
- **One ledger is guarded at four spans on purpose, and the thinness is the finding.** `california-state-bellwethers` covers JCCP 5255 in the Los Angeles Superior Court, and **California state court records are not in any free archive**: no RECAP for LASC, and the Court of Appeal portal blocks automated lookups. The court's own public notice is the only primary the project can hold, so the scope is section (a) and nothing else. Registering it anyway was deliberate: "NOT checked, no allowlist authored" reads like undone work, while "PARTIALLY checked, only section (a)" states the actual shape of the evidence. It is worth saying plainly, because this is the case where the project once published "upheld on appeal" about a verdict no appellate court had reviewed.
- **`npm run check:surfaces` guards the surfaces no other checker can see, added 2026-08-14.** `check:ledgers` proves the TLDR variants reach their consumers and `check:quotes` proves quoted spans are verbatim; **neither can notice that an OG card chip describes a decided case as pending**, because OG cards and JSON-LD are TSX and the ledger-first re-seed pass only edits `content/`. That is exactly how five surfaces spent three weeks telling crawlers, agents and social previews that New Mexico's abatement phase was still open. The guard does two different things: **figures** (every dollar amount on a case's share card must appear in that case's ledger, structural) and **posture** (words asserting a matter is undecided (`pending`, `requested`, `weighing`, `awaiting`, `sought`, `proposed`, `seeking`) must be allowlisted with a reason, blunt on purpose). **The vocabulary half is scoped hard to share cards and structured data and must stay that way**: prose is allowed to discuss pending things, and this repo has been burned by one comment becoming a standing multi-file gate. The live allowlist entries each carry a reason, and the count is deliberately not stated here because it has already gone stale once: it read "three" until 2026-08-20, when the $1.4T entry was deleted. **A stale allowlist entry is now a hard failure**, added the same day: every entry must still match a word actually on its surface, because an entry excusing a word that is not there asserts a considered justification for nothing, and this repo has written down that an allowlist is where a real defect goes to hide. It was found by accident rather than by the checker, which printed the dead entry as live. Fault-injected and confirmed to fail on the exact 2026-08-14 defect (the original stale chips), on a plausible invented figure ($599M), and to **abort rather than report green** when its own matcher is broken. **One subtlety it got wrong on the first draft, caught in code review the same day:** the corpus test made the dollar sign optional (`\$?`), which quietly reduced "does the ledger assert this dollar amount" to "does this number appear anywhere", and the ledgers are dense with claim numbers, paragraph references and dates. `see page 567 of the order` satisfied a `$567M` chip. The `$` is now required, and the self-test carries a **third control** asserting that a bare integer does NOT satisfy a money figure, so the degradation cannot come back silently.
- **`oneParagraph` is parsed by `src/lib/distillations.ts` but read by nothing on the site.** That is deliberate: its only consumer is the hand-copied page opener, and parsing it in the lib is what lets the checker share the build's parser. It is `required` for `kind: "lawsuit"` so it can never be blank, because `"".includes()` is true of every string and a blank variant would make the opener check pass vacuously.

- **Deploy**: Railway auto-deploys `main` (roughly 45s to 2min), fronted by Cloudflare. Verify user-facing changes live on de-amplify.com after the deploy, not just in the local build.
- **CSP**: security headers, including a scoped Content-Security-Policy, live in `next.config.mjs`. If you add an external embed or resource, update the CSP or the browser will block it at runtime. The one current external dependency is the Suno iframe (allowed via `frame-src`). Check the browser console for CSP violations after deploying.
- **Railway port gotcha**: the custom-domain target port must match the injected `$PORT` (8080). Do not hardcode `-p`. See the "Port note" in the README.

## Where an open thread goes (four homes, and the boundary is the point)

Added 2026-08-18, when `docs/issues/` was created as a general convention. **A fourth tracking
location is a real risk in a repository whose most persistent failure is a fact living somewhere
nobody re-reads**, so the boundary is narrow and it is checked before adding anything:

- **A question about the evidentiary record of one proceeding** goes in that ledger's **Tensions** section. There are more than sixty and they work: they are published with the ledger and read on every re-seed of that case. "Is the trial bifurcated" is a big question and it lives there.
- **Work being done now, with a shape and an order**, goes in a dated plan in `docs/plans/`. Plans close, and their follow-up lists are the outcome of an exercise rather than a standing queue.
- **A quoted span that cannot verify against its cache** goes in `KNOWN_DEVIATIONS` in `scripts/check-quotes.mjs`, which prints it on every run.
- **Everything else genuinely open** goes in `docs/issues/`, one dated file per thread, indexed, guarded by `npm run check:issues`. **That guard matched index rows to files and did not read the status column until 2026-08-20**, so it printed OK while the index described a closed thread as open. Presence in both directions was never the interesting half: an index whose rows all exist can still describe every one of them wrongly. The `Status` cell is now asserted against each file's own frontmatter, and a row without a readable status cell is itself a failure, so the table format is load-bearing. **A closed issue must also carry `revisited:`, accounting for every path in its `related:` list with a disposition (`updated` or `no-change-needed`) and a reason.** This is the repo's only guard on **prose staleness in `docs/research/` and `docs/plans/`**, and its shape is deliberate. Closing a thread falsifies sentences in the documents that depended on it, and no checker here can see that: they all read machine-readable surfaces. **A scan of research prose for words like "still" or "not obtained" is the wrong instrument and is explicitly ruled out**, for the reason the `check:surfaces` vocabulary is scoped hard to share cards: prose is allowed to discuss pending things, a research corpus is largely honest sentences about open questions, and one comment becoming a standing multi-file gate is a documented injury here. So the check asserts that **somebody looked**, at close time, in a place the answer is preserved. It cannot prove the disposition is true, and says so every run. Calibrated before it was written: of nine related-file pairs across five closed issues it would have fired on **two**, one of which was a real gap (the MDL ledger said nothing about *Doe 1*, the circuit's other Section 230 case, decided a week apart before the same district judge; now claim 43). **A rule that fires on two of nine is worth having; one firing on all nine would be noise.** The same guard also **recomputes the ledger-Tension count** that `docs/issues/README.md` declares, because that number was hand-written as 62 and was already wrong at the commit that wrote it (63 then, 64 on 2026-08-20). **That is the third count found living only in prose**, after the quote-span total and the guard list, and it got the same fix all three times: one declared home, recomputed by a tool. The counter aborts rather than comparing against zero if it finds no Tensions sections.

The test is **ownership, not importance**. Michigan's withdrawal is smaller than the bifurcation
question and belongs in issues, because no ledger claims it and the plan that found it has closed.

Three frontmatter fields are mandatory and each encodes a lesson this project paid for.
`closes_when` stops an issue being a mood. `trigger` is the press-conference finding: that item read
as dead research through two retries when the real state was one command away, so **a trigger beats
a reminder** and `none` is a real answer. `surface` is the plan's own insight that **verified is not
the same as publishable**, asked at open time rather than after the research.

**The checker cannot tell whether an issue duplicates a ledger Tension**, which is the failure this
directory most needs to avoid, and it says so on every run rather than letting green imply an answer
it does not have.

## Known open threads

- ~~**`contentDate()` is inert in production, so every date a crawler sees is a stale hardcoded literal.**~~ **FIXED 2026-08-02 by removing the dependency, after the obvious fix failed.** Worth reading in full, because the sequence is the lesson.

  The build shelled out to `git log`. That worked locally and silently did not work on Railway: every JSON-LD `dateModified` and sitemap `lastmod` was a hardcoded fallback, and the live site claimed nothing had changed since 2026-07-25 through three weeks of corrections. **Green builds throughout**, because a wrong date is not a compile error. It was found by diffing a live sitemap against a local build, not by anything in the pipeline.

  First fix attempt: `nixPkgs` replaces the default package set, so the image had no `git`; adding it was a one-line change backed by a **reproduction** (building with `git` off `PATH` emits exactly the three dates production was serving). **It did not work.** The symptom matched, which is not the same as having found this system's cause, and the true cause was never observable from outside Railway's build logs.

  Real fix: the build no longer asks the container anything. `npm run dates` resolves dates on a machine that has git and full history and writes `content-dates.json`, which is committed and **imported** (not `fs`-read, so no cwd or file-tracing dependency). Verified by building with `git` entirely absent: zero warnings, correct dates. `npm run check:dates` fails when the manifest drifts from git, so it cannot rot silently the way the original did.

  **Two things that fell out of it, both worth keeping:**
  - `/api/health` now reports date provenance (`manifestGenerated`, `manifestEntries`, a live resolved `sample`, and `coverage`). The entire three-week failure was invisible without build-log access; it is now one `curl`. **The block was corrected on 2026-08-13, because the part meant to catch a regression could not.** It reported `resolvedFrom: {manifest, git, fallback}` counters, which looked like a build-wide tally and were nothing of the sort: every page is prerendered during `next build`, in a different process from the one serving traffic, so the running server's counters only ever recorded the health route's own lookups and would read `fallback: 0` on a deploy whose every page had fallen back. A counter that cannot go red is not provenance. Those counters are still there, honestly renamed `lookupsThisProcess`; the load-bearing field is now `coverage`, which asks the **shipped** manifest whether it carries every content path the dated surfaces need and names the ones it does not. Fault-injected by deleting two manifest keys: `ok` goes false and `missing` names both.
  - Call sites pass **both** absolute (`path.join(process.cwd(), ...)`) and repo-relative paths. `git` accepted either, so the inconsistency never mattered and both styles are still in the tree; a manifest lookup does not, and `contentDate` normalises. That mismatch only became visible once git was gone to stop papering over it. The live sitemap's `lastmod` values and every JSON-LD `dateModified` are exactly the fallback strings passed at the call sites (`2026-07-15`, `2026-07-16`, `2026-07-25`), and the deployed site carries **no date later than 2026-07-25** even for files committed since. The identical build run locally emits correct git author dates. Diagnosis, stated at the confidence it deserves: `nixpacks.toml` pins `nixPkgs = ["nodejs_20"]`, so `git` is most likely absent from the build image and every `execFileSync("git", ...)` throws straight into its fallback; a build context without a `.git` directory would produce the same symptom, and the two cannot be told apart from outside Railway. **This matters more than it looks** on a site whose entire proposition is dated, frequently corrected legal content: every correction shipped since mid-July is invisible to crawlers as a freshness signal. Cheapest probe is to add `"git"` to `nixPkgs`, redeploy, and see whether any date moves; it is self-diagnosing. Do not "fix" it by reintroducing mtime, which is the failure the mechanism exists to prevent.

- The two tier-down hearing ledgers (May 2026 Senate verdicts, Dec 2025 House legislative solutions) are built from witness written testimony. **Dec 2025's transcript published and is cached** (`CHRG-119hhrg62241`, fetched 2026-07-25), which narrowed but did not close that tier-down, and the reason is a trap worth remembering: **a GPO HTML transcript carries the spoken hearing only. Prepared statements are inserted as TIFF images and render as the literal string `[GRAPHIC NOT AVAILABLE IN TIFF FORMAT]`, which no substring check can reach.** Grep that marker first on any new GPO hearing; it says in one command how much of the record is unreachable. Worse for that hearing, one witness's statement (Ruane) is not in the official record at all, only footnoted to a committee-repository URL. So publication is not the same as verifiability: of that ledger's 37 quoted spans, only 5 touch the official transcript. The remaining work is a re-seed from the live Q&A, not a banner flip, and it is worth doing because the spoken record also contains quotable material the written testimony lacks. May 2026 is still awaiting its transcript. Expect the same split when it lands.
- ~~**The EU Digital Services Act proceeding has no ledger.**~~ **CLOSED 2026-08-02.** `docs/distillations/eu-dsa-proceedings.md` is built from all four Commission releases (IP/26/312, IP/26/920, IP/26/1579, IP/26/1679), each cached in `sources/`, with every quoted span verified as an exact substring under punctuation folding. Ledger-first now holds everywhere on the site. Two things that pass left behind, both worth knowing before touching this source family:
  - **A plain fetch of an `ec.europa.eu/commission/presscorner/detail/...` URL returns HTTP 200 and no article text.** The page is a JavaScript shell; the headline survives in meta tags, so a naive extraction yields a document with the right title and an empty body, which is a false success rather than a visible failure. The body is served as JSON by the Commission's own API at `.../presscorner/api/documents?reference=IP/26/1579&language=en` (field `docuLanguageResource.htmlContent`), and releases are located by `.../presscorner/api/search?language=en&text=...`. Reference numbers are **not** chronological (IP/26/312 is Feb 6, IP/26/310 is Feb 9), so enumerating them around a date does not work; search instead.
  - **The unverified claim that ledger deliberately did not make.** The parent plan asserts no DSA fine to date rests on a user-control or recommender-transparency count (X, Temu and AliExpress being risk-assessment, dark-pattern, ad-transparency and researcher-access matters). That is load-bearing and was **not** verified in the 2026-08-02 pass. It sits in the ledger's Tensions as an open question and must not be stated on the site until the three decisions themselves are fetched.
- **A third distillation `kind` exists now: `regulatory`.** It is not cosmetic. `/lawsuits` insists the Commission's proceeding "is not a lawsuit at all but a regulatory process", and `kind` drives a reader-facing group heading on `/distillations`, so folding the EU ledger into `lawsuit` would have contradicted the site's own distinction in public. `regulatory` does not require a `ledger snippet` (same rationale as hearings: its curated surface is a section of `/lawsuits`, so there is no per-case page meta description to collide with), though the EU ledger authors one anyway. **There are two `regulatory` ledgers as of 2026-08-14**: the EU DSA proceedings and `ftc-control-integrity`. The second stretches the kind, deliberately and worth knowing: it is not one proceeding but **four FTC matters** (the Eighth Circuit's vacatur of the click-to-cancel rule, Amazon Prime, Epic, and Chitika), gathered because together they show an American regulator already writing persistence, friction and scope into enforceable order terms for everything except a feed. It is a **record of a remedy template**, not a case, so it appears on `/lawsuits` as a section under what the lawsuits cannot fix rather than in the map of proceedings, and the site's "six proceedings" count deliberately excludes it.
